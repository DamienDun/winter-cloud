<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.winter</groupId>
    <artifactId>winter-cloud</artifactId>
    <version>3.6.7</version>

    <name>winter-cloud</name>
    <description>winter微服务</description>

    <properties>
        <winter.version>3.6.7</winter.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <spring-boot.version>2.7.18</spring-boot.version>
        <spring-cloud.version>2021.0.9</spring-cloud.version>
        <spring-cloud-alibaba.version>2021.0.6.1</spring-cloud-alibaba.version>
        <spring-boot-admin.version>2.7.16</spring-boot-admin.version>
        <tobato.version>1.27.2</tobato.version>
        <kaptcha.version>2.3.3</kaptcha.version>
        <pagehelper.boot.version>2.0.0</pagehelper.boot.version>
        <druid.version>1.2.27</druid.version>
        <dynamic-ds.version>4.3.1</dynamic-ds.version>
        <commons.io.version>2.21.0</commons.io.version>
        <velocity.version>2.3</velocity.version>
        <fastjson.version>2.0.61</fastjson.version>
        <jjwt.version>0.9.1</jjwt.version>
        <minio.version>8.2.2</minio.version>
        <poi.version>4.1.2</poi.version>
        <springdoc.version>2.1.0</springdoc.version>
        <transmittable-thread-local.version>2.14.4</transmittable-thread-local.version>
        <!-- override dependency version -->
        <tomcat.version>9.0.112</tomcat.version>
        <logback.version>1.2.13</logback.version>
        <spring-framework.version>5.3.39</spring-framework.version>
        <lombok.version>1.18.22</lombok.version>
        <knife4j.version>4.3.0</knife4j.version>
        <mybatisplus.version>3.5.1</mybatisplus.version>
        <mybatisplusjoin.version>1.2.4</mybatisplusjoin.version>
        <modelmapper.version>2.3.4</modelmapper.version>
        <tencent.cos.client.version>5.6.25</tencent.cos.client.version>
        <huawei.obs.client.version>3.20.4.2</huawei.obs.client.version>
        <fastdfs.client.version>1.27</fastdfs.client.version>
        <minio.client.version>7.0.2</minio.client.version>
        <aliyun.sdk.oss.version>3.4.2</aliyun.sdk.oss.version>
        <aliyun.sdk.core.version>4.3.1</aliyun.sdk.core.version>
        <dingtalk.old.version>2.0.0</dingtalk.old.version>
        <dingtalk.new.version>1.4.15</dingtalk.new.version>
        <spring.security.oauth2.version>2.3.8.RELEASE</spring.security.oauth2.version>
        <security.jwt.version>1.0.9.RELEASE</security.jwt.version>
        <postgresql.jdbc.version>42.2.5</postgresql.jdbc.version>
        <hive.jdbc.version>2.1.0</hive.jdbc.version>
        <oracle.jdbc.version>21.1.0.0</oracle.jdbc.version>
        <sqlserver.jdbc.version>4.0</sqlserver.jdbc.version>
        <presto.jdbc.version>0.240</presto.jdbc.version>
        <clickhouse.jdbc.version>0.2.4</clickhouse.jdbc.version>
        <redisson.version>3.16.3</redisson.version>
        <proguard.version>6.2.2</proguard.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- 覆盖SpringFramework的依赖配置-->
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-framework-bom</artifactId>
                <version>${spring-framework.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- SpringCloud 微服务 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- SpringCloud Alibaba 微服务 -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- SpringBoot 依赖配置 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 覆盖logback的依赖配置-->
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!-- 覆盖tomcat的依赖配置-->
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>

            <!-- FastDFS 分布式文件系统 -->
            <dependency>
                <groupId>com.github.tobato</groupId>
                <artifactId>fastdfs-client</artifactId>
                <version>${tobato.version}</version>
            </dependency>

            <!-- Springdoc webmvc 依赖配置 -->
            <!--            <dependency>-->
            <!--                <groupId>org.springdoc</groupId>-->
            <!--                <artifactId>springdoc-openapi-ui</artifactId>-->
            <!--                <version>${springdoc.version}</version>-->
            <!--            </dependency>-->

            <!-- 验证码 -->
            <dependency>
                <groupId>pro.fessional</groupId>
                <artifactId>kaptcha</artifactId>
                <version>${kaptcha.version}</version>
            </dependency>

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.boot.version}</version>
            </dependency>

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- 代码生成使用模板 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- JSON 解析器和生成器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <!-- 线程传递值 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId>
                <version>${transmittable-thread-local.version}</version>
            </dependency>

            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatisplus.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>com.baomidou</groupId>
                        <artifactId>mybatis-plus-generator</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- MyBatis plus 连表查询 -->
            <dependency>
                <groupId>com.github.yulichang</groupId>
                <artifactId>mybatis-plus-join</artifactId>
                <version>${mybatisplusjoin.version}</version>
            </dependency>

            <!-- Knife4j 核心依赖（JDK 8 适配，非 jakarta 版本） -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <dependency>
                <groupId>org.modelmapper</groupId>
                <artifactId>modelmapper</artifactId>
                <version>${modelmapper.version}</version>
            </dependency>

            <!-- 核心模块 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-core</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 接口模块 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-swagger</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 安全模块 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-security</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 数据脱敏 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-sensitive</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 权限范围 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-datascope</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 多数据源 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-datasource</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 分布式事务 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-seata</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 日志记录 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-log</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 缓存服务 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-common-redis</artifactId>
                <version>${winter.version}</version>
            </dependency>

            <!-- 系统接口 -->
            <dependency>
                <groupId>com.winter</groupId>
                <artifactId>winter-cloud-api-system</artifactId>
                <version>${winter.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>winter-cloud-auth</module>
        <module>winter-cloud-gateway</module>
        <module>winter-cloud-visual</module>
        <module>winter-cloud-modules</module>
        <module>winter-cloud-api</module>
        <module>winter-cloud-common</module>
    </modules>
    <packaging>pom</packaging>

    <dependencies>
        <!-- bootstrap 启动器 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <repositories>
        <repository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
        <!-- Knife4j 专属仓库（可选，确保 Knife4j 依赖下载） -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
